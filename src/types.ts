export const enum TimeUnit {
  Millisecond,
  Second,
  Minute,
  Hour,
  Day,
  Month,
}

export const enum TimeUnitInSeconds {
  Millisecond = 0.001,
  Second = 1,
  Minute = 60,
  Hour = 3600,
  Day = 86400,
  Month = 2592000,
}
